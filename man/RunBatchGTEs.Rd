% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch_other.R
\name{RunBatchGTEs}
\alias{RunBatchGTEs}
\title{Run GTEs Analysis}
\usage{
RunBatchGTEs(
  object,
  assay = "RNA",
  slot = "data",
  group.by = NULL,
  batch.by,
  block.size = 500
)
}
\arguments{
\item{object}{A single-cell object (e.g., Seurat object) from which
expression data and metadata will be extracted.}

\item{assay}{A character string specifying the assay to use (e.g., "RNA").
Defaults to "RNA".}

\item{slot}{A character string specifying the slot to use for expression
data (e.g., "data"). Defaults to "data".}

\item{group.by}{(Optional) A character string specifying the metadata column
representing biological groups (e.g., cell type, condition) to be
controlled for. Defaults to \code{NULL}.}

\item{batch.by}{A character string specifying the metadata column representing
the batch variable whose influence is to be quantified (e.g., "batch_id").}

\item{block.size}{An integer specifying the number of genes to process in
each block. This helps manage memory for large datasets. Defaults to 500.}
}
\value{
A data frame with two columns: 'Feature' (gene names) and column
named after \code{batch.by} (representing the GTE score for that feature).
Higher scores indicate a stronger association with the batch effect.
}
\description{
This function calculates GTEs scores from GTEs package
}
\details{
This function utilizes the \code{GTEs::Run.GroupTechEffects} function to compute GTE
scores. It iterates through genes in \code{block.size} chunks to avoid memory
overflow, especially with large expression matrices.
The \code{batch.by} variable is the technical factor of interest.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PlotFeature.R
\name{PlotFeatureCosineCiteSeq}
\alias{PlotFeatureCosineCiteSeq}
\title{Plot cosine similarity between RNA and ADT features in CITE-Seq data}
\usage{
PlotFeatureCosineCiteSeq(
  object,
  RNA_name = NULL,
  ADT_name = NULL,
  slot = "data",
  return.type = "plot",
  color = "#8B658B",
  ntop = 15
)
}
\arguments{
\item{object}{A Seurat object containing RNA and ADT data. This object is required for extracting the specified features and calculating cosine similarity.}

\item{RNA_name}{A character vector of RNA feature names. These features will be compared with ADT features for cosine similarity calculation.}

\item{ADT_name}{A character vector of ADT feature names corresponding to the RNA features specified in \code{RNA_name}.}

\item{slot}{Character string specifying which slot to use for feature data (e.g., \code{"data"}, \code{"counts"}, or \code{"scale.data"}). Default is \code{"data"}.}

\item{return.type}{Character string or vector indicating the type of output to return. Options are \code{"plot"}, \code{"interactive_table"}, or both. Default is \code{"plot"}.}

\item{color}{Character string specifying the color used in the plot. Default is \code{"#8B658B"}.}

\item{ntop}{Numeric value indicating the number of top feature pairs to highlight in the bar plot based on their cosine similarity. Default is \code{15}.}
}
\value{
The function returns either a \code{ggplot} object, an interactive table, or both, depending on the \code{return.type} specified.
\itemize{
\item If \code{"plot"} is specified, a combined \code{ggplot} object showing a bar plot of the top \code{ntop} cosine similarities and a histogram of all cosine similarities is returned.
\item If \code{"interactive_table"} is specified, an interactive table summarizing the cosine similarity between RNA and ADT features is returned.
\item If both are specified, a list containing both the plot and the interactive table is returned.
}
}
\description{
This function calculates and plots the cosine similarity between specified RNA and ADT features in a Seurat object from CITE-Seq data.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PlotFeature.R
\name{PlotFeatureMeanVariance}
\alias{PlotFeatureMeanVariance}
\title{Plot mean-variance plot by sample}
\usage{
PlotFeatureMeanVariance(
  object,
  assay = "RNA",
  mean_col = "mean_lognorm",
  var_col = "variance_lognorm",
  return.type = c("plot", "plotly")
)
}
\arguments{
\item{object}{A list object gets from \code{CalculateMetricsPerFeature} function.}

\item{assay}{Character string specifying which assay to use. Defaults to \code{"RNA"}.}

\item{mean_col}{Column name that stores the per-feature means. Defaults to \code{"mean_lognorm"}.}

\item{var_col}{Column name that stores the per-feature variances. Defaults to \code{"variance_lognorm"}.}

\item{return.type}{Character string specifying the type of object to return.
Can be \code{"plot"} (default) to return a ggplot object, or \code{"plotly"}
to return \code{plotly::ggplotly()}.}
}
\value{
A ggplot object if \code{return.type = "plot"}, or a plotly object
if \code{return.type = "plotly"}.
}
\description{
For each sample within an assay, this function extracts feature-level
mean and variance statistics and plots the mean-variance relationship
using a GAM smoother. Optionally, the ggplot object can be converted
to an interactive plotly plot.
}
